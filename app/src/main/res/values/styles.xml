<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorParvozPerple2</item>
        <item name="colorPrimaryDark">@color/colorParvozPerple2</item>
        <item name="colorAccent">@color/colorParvozPerple2</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="style_Spinner">
        <item name="android:dropDownSelector">@android:color/transparent</item>
        <item name="android:popupBackground">#a6417e</item>
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:bottomRightRadius">15dp</item>
        <item name="android:colorBackground">#a6417e</item>

    </style>
</resources>
